logging:
  version: 1

  format:
    standard: "%(asctime)s | %(levelname)s | %(name)s | %(message)s"

  handlers:
    console:
      enabled: true

    file:
      enabled: true
      path: logs/agpp.log

  development:
    level: DEBUG

  testing:
    level: WARNING

  production:
    level: INFO